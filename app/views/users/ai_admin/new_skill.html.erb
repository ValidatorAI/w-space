<% @page_title = "AI Config - Add Skill" %>
<% @body_class = "sidebar" %>

<% content_for :nav do %>
  <span class="btn btn--reversed btn--faux user-settings-nav__pill bg-black">Workspace</span>
  <h1 class="user-settings-nav__title txt-large">Add Skill</h1>
<% end %>

<% content_for :sidebar, sidebar_turbo_frame_tag(src: user_sidebar_path) %>

<div id="message-area" class="message-area">
  <div class="feed-container company-settings-feed">
    <div class="settings-container">
      <%= render "users/ai_admin/top_links", active_tab: :skills %>

      <section class="settings-card" aria-label="Add skill form">
        <div class="settings-section-title settings-section-title--spread">
          <span>Add Skill</span>
          <%= link_to "Back to skills", user_company_ai_admin_skills_page_path(user_id: "me"), class: "action-btn action-btn--small" %>
        </div>

        <%= render "users/ai_admin/skill_form",
            skill: @skill,
            form_url: user_company_ai_admin_skills_path(user_id: "me"),
            form_method: :post,
            submit_label: "Add skill" %>
      </section>
    </div>
  </div>
</div>

# Copy this file to .env and replace the token with the value accepted by the receiving service.
OUTPUT_EVENTS_URL=http://localhost:8031/space_events
OUTPUT_EVENTS_TOKEN=replace-with-your-shared-token